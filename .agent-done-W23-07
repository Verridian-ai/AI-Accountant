Wave 23 Agent 7: API Endpoints Builder — COMPLETE

26 API routes added to server/src/index.ts:

TENANT ROUTES (6):
  POST   /api/tenants                              — Create tenant
  GET    /api/tenants                              — List user's tenants
  GET    /api/tenants/:id                          — Get tenant
  PUT    /api/tenants/:id                          — Update tenant (settings.manage)
  POST   /api/tenants/:id/switch                   — Switch tenant (new JWT)
  DELETE /api/tenants/:id                          — Deactivate (owner only)

MEMBER ROUTES (6):
  GET    /api/tenants/:tenantId/members            — List members (members.read)
  POST   /api/tenants/:tenantId/members            — Add member (members.manage)
  PUT    /api/tenants/:tenantId/members/:userId/role — Update role (members.manage)
  DELETE /api/tenants/:tenantId/members/:userId     — Remove member (members.manage)
  POST   /api/tenants/:tenantId/invitations        — Send invitation (members.manage)
  POST   /api/invitations/accept                   — Accept invitation (public)

PERMISSION ROUTES (4):
  GET    /api/tenants/:tenantId/permissions         — List permissions
  GET    /api/tenants/:tenantId/permissions/matrix  — Permission matrix
  PUT    /api/tenants/:tenantId/permissions/:role   — Update role perms (owner)
  POST   /api/tenants/:tenantId/permissions/reset   — Reset defaults (owner)

SUBSCRIPTION ROUTES (6):
  GET    /api/subscriptions/plans                   — List plans (public)
  GET    /api/tenants/:tenantId/subscription        — Get subscription+usage
  POST   /api/tenants/:tenantId/subscription        — Subscribe (settings.manage)
  PUT    /api/tenants/:tenantId/subscription/upgrade  — Upgrade (settings.manage)
  PUT    /api/tenants/:tenantId/subscription/downgrade — Downgrade (settings.manage)
  DELETE /api/tenants/:tenantId/subscription        — Cancel (settings.manage)

AUTH ROUTES (4):
  POST   /api/auth/login                           — Tenant-aware login
  POST   /api/auth/register                        — Register + optional tenant
  POST   /api/auth/refresh                         — Refresh/switch tenant token
  GET    /api/auth/me                              — Current user+tenant context

IMPORTS ADDED:
  - tenantService from ./services/tenant.js
  - rbacService from ./services/rbac.js
  - subscriptionService from ./services/subscriptions.js
  - ForbiddenError from ./services/rbac-errors.js
  - adminAuthService from ./services/admin-auth.js

PUBLIC PATHS ADDED:
  - /api/subscriptions/plans, /api/invitations/accept
  - /api/auth/login, /api/auth/register, /api/auth/refresh

VERIFICATION:
  - tsc --noEmit: zero errors in index.ts (pre-existing errors in other files only)
  - No route path conflicts with existing routes
  - RBAC enforcement on all protected routes
  - Backwards-compatible auth (legacy + tenant-aware JWT)
