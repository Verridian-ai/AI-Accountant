Agent 02 — Mutation Tools Service — COMPLETE
Wave 2, Sub-Wave 1

Created: server/src/services/claude/mutation-tools.ts

Exports:
  - MutationTools class
  - MutationProposal interface
  - AgentMutation interface
  - MutationStatus type
  - MutationExecutionResult interface

Key features:
  - proposeMutation() — creates tracked mutation record, broadcasts SSE event
  - batchProposeMutations() — batch version for bulk operations
  - executeMutation() — applies confirmed mutations to DB with error handling
  - getMutation() / getSessionMutations() / getPendingMutations() — query helpers
  - expireStale() — expires unconfirmed mutations past 15-min TTL
  - updateMutationStatus() — status transitions with extra field support

Security (D01-CRIT-01):
  - MUTABLE_TABLES whitelist (34 tables)
  - SAFE_IDENTIFIER_RE regex for table/column validation
  - validateTableName() rejects invalid/unlisted tables before SQL
  - validateColumnNames() rejects invalid column identifiers
  - Injection attempt like 'users; DROP TABLE--' throws error

Design decisions:
  - Uses crypto.randomUUID() (project convention, no uuid package)
  - Uses events.emit('update', ...) from existing events.ts pattern
  - rowToMutation() handles both snake_case and camelCase columns
  - Boolean stored as 0/1 for SQLite compatibility
  - Non-fatal session count increment (session may not exist yet)

TypeScript: ZERO new errors (pre-existing errors in tool-adapter.ts and confirmation-flow.ts are unrelated)
No existing files modified.

Timestamp: 2026-02-13
