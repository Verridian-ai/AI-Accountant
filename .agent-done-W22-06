Wave 22 Agent 6 (dashboard-schema-builder) — DONE

Created:
1. docker/migrations/0034_custom_dashboards.sql — PostgreSQL migration with 2 tables + 5 indexes
2. server/src/schema.ts — Added dashboardLayouts + savedCharts sqliteTable definitions + 4 type exports
3. server/src/db/postgres-schema.ts — Added dashboardLayouts + savedCharts pgTable definitions + 4 type exports

Tables:
- dashboard_layouts: id, user_id, name, description, layout_json, widgets, is_default, is_shared, thumbnail_url, sort_order, created_at, updated_at
- saved_charts: id, user_id, dashboard_id (FK → dashboard_layouts), chart_type, title, description, data_source, config_json, filters_json, refresh_interval_seconds, pinned, sort_order, created_at, updated_at

Type check: All 13 TS errors are pre-existing in vercel-agent/tool-adapter/vercel-provider files. Zero schema-related errors.
