Agent 06 (Audit Trail Service) — Wave 2 — COMPLETE

Created:
- server/src/services/claude/audit.ts

Exports:
- AuditService class
- AuditEntry interface
- AuditAction type
- AuditQueryOptions interface

Features:
- 10 audit actions: mutation lifecycle (7) + query + tool + error
- Append-only design: only INSERTs, no UPDATE/DELETE
- Non-throwing: audit failures caught internally, never propagated
- PII redaction (D02-SEC-07): TFN, BSB, bank account number patterns stripped from query results
- Parameterized SQL queries for injection safety
- Column aliasing for camelCase TypeScript interfaces
- Pagination support in queryAudit() (limit/offset with total count)
- getMutationAuditTrail() for full mutation lifecycle view
- getSessionAuditTrail() for session-scoped audit history
- Uses crypto.randomUUID() (native Node.js, no uuid dependency)

TypeScript: ZERO new errors (pre-existing errors in tool-adapter.ts and confirmation-flow.ts are unrelated)

Blocks: Agent 7 (API endpoints need AuditService for /api/agent-audit)
