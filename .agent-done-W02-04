Agent 04 — Confirmation Flow Service — COMPLETE

Created: server/src/services/claude/confirmation-flow.ts

Exports:
  - ConfirmationFlowService (class)
  - AgentSession (interface)
  - CreateSessionOptions (interface)

Methods:
  - getOrCreateSession(options?) — create/reuse active session
  - createMutationTools(sessionId) — factory for MutationTools bound to session
  - proposeAndBroadcast(sessionId, agentType, proposal) — auth + propose + auto-exec + SSE
  - confirm(mutationId, userId, reason?) — validate + execute + audit (D02-CRIT-02: userId required)
  - reject(mutationId, userId, reason?) — reject + audit (D02-CRIT-02: userId required)
  - expireStale() — expire unconfirmed mutations past TTL
  - getPendingMutations(sessionId) — query pending mutations
  - getSessionHistory(options?) — paginated session history
  - getSession(sessionId) — single session lookup
  - incrementQueryCount(sessionId) — bump query counter
  - recordAgentUsage(sessionId, agentType) — track agent types used

Security:
  - D02-CRIT-02: confirm/reject validate session ownership via validateSessionOwnership()
  - D01-DC-05: Consistent error handling with descriptive messages

Dependencies:
  - MutationTools from ./mutation-tools.js (Agent 02)
  - MutationAuthService from ./mutation-auth.js (Agent 03)
  - events from ../../events.js (global SSE emitter)
  - AgentType from ./types.js

TypeScript: 0 errors from this file (verified via npx tsc --noEmit)
No existing files modified.
