# =============================================================================
# PHASE FINAL: Zero-Any Cleanup + Git Commit + Full Stack Verification
# =============================================================================
# GOAL: Every file committed to main, 0 TS errors, 0 untyped `any`,
#       Docker stack green, Neon Cloud verified with 6520 transactions.
# =============================================================================

## CURRENT STATE (verified before launch)

### TypeScript
- Server: **0 TS errors** (npx tsc --noEmit passes clean)
- Client: **0 TS errors** (npx tsc --noEmit passes clean)
- Server `any` count: **2** (eslint-disabled Proxy wrappers in schema.ts — ACCEPTABLE)
- Client `any` count: **453 across 46 files** — MUST BE ELIMINATED

### Client `any` Hotspots (attack these first)
| File | `: any` count |
|------|-------------|
| `client/src/api.ts` | 198 |
| `client/src/api/misc.ts` | 157 |
| `client/src/features/admin/components/CogneeGraphViewer.tsx` | 8 |
| `client/src/features/admin/components/CogneeGraph2DFallback.tsx` | 8 |
| `client/src/features/payroll/components/EmployeeDetail.tsx` | 6 |
| `client/src/features/knowledge/components/DataPointManager.tsx` | 5 |
| `client/src/features/analytics/components/WealthProjection.tsx` | 5 |
| 39 other files | 1-4 each |

### Git State
- Branch: `refactor/REFACTOR-018-account-service`
- Modified files: 116
- Untracked new files: 45
- NONE committed yet — all changes from Phase A agents are unstaged

### Docker
- All 5 services running: postgres, cognee, redis, server, client
- Health checks: all healthy

### Neon Cloud
- Project: GoldLedger (dawn-hall-65417424) in aws-ap-southeast-2
- 53 tables migrated, 6520 transactions, 28 statements, 2 accounts, 2 users
- Extensions: anon v2.5.1, uuid-ossp, pg_trgm
- Connection: postgresql://neondb_owner:npg_wmEJX7uUcHp3@ep-steep-waterfall-a7j76g5z.ap-southeast-2.aws.neon.tech/neondb?sslmode=require

---

## AGENT ROSTER: 3 Agents

### Agent 1: `any-killer-client` — Eliminate ALL `any` from client (453 -> 0)

**Mission**: Replace every `: any` in `client/src/` with proper types.

**Strategy**:
1. Start with `client/src/api.ts` (198 occurrences) — this is the API layer. Create proper interfaces for every API response/request. Look at the actual server route handlers to determine correct types.
2. Then `client/src/api/misc.ts` (157 occurrences) — these are stub APIs. Create proper typed interfaces for each.
3. Work through the remaining 44 files, creating interfaces as needed.

**Rules**:
- NEVER use `any`. Use `unknown`, proper interfaces, or generic types.
- NEVER use `as any` casting. Use proper type narrowing.
- If a type is genuinely unknown at compile time, use `unknown` with type guards.
- For API responses, create interfaces in a shared `client/src/types/` directory.
- For event handlers, use React's proper event types (e.g., `React.ChangeEvent<HTMLInputElement>`).
- For third-party library callbacks, use the library's exported types.

**Verification**:
```bash
npx tsc --noEmit  # must pass with 0 errors
grep -rn ': any\b' src/ --include='*.ts' --include='*.tsx' | grep -v test | grep -v '.d.ts' | grep -v 'eslint-disable' | grep -v node_modules | wc -l  # must be 0
```

**HARD DELIVERABLE**: `grep -rn ': any' client/src/ --include='*.ts' --include='*.tsx' | grep -v 'eslint-disable' | wc -l` returns 0. `npx tsc --noEmit` passes.

---

### Agent 2: `docker-verifier` — Rebuild Docker + Verify Full Stack

**Mission**: Clean rebuild of entire Docker stack, verify all services, verify Neon connectivity.

**Tasks**:
1. Run `docker compose down` then `docker compose build --no-cache`
2. Run `docker compose up -d`
3. Wait for all healthchecks to pass
4. Verify endpoints:
   - `curl http://localhost:3501/health` — server health
   - `curl http://localhost:8080` — client loads
   - `curl http://localhost:8000/api/v1/settings` — cognee responds
5. Verify Neon Cloud connectivity from the server container:
   ```bash
   docker compose exec -T server node -e "
     const pg = require('pg');
     const c = new pg.Client({connectionString: process.env.NEON_DATABASE_URL});
     c.connect()
       .then(() => c.query('SELECT count(*) as c FROM transactions'))
       .then(r => { console.log('Neon transactions:', r.rows[0].c); return c.end(); })
       .catch(e => { console.error('NEON ERROR:', e.message); process.exit(1); });
   "
   ```
   Must return `Neon transactions: 6520`
6. Fix ANY Docker build errors, healthcheck failures, or connectivity issues.

**HARD DELIVERABLE**: `docker compose ps` shows all 5 services healthy. Neon query returns 6520 transactions.

---

### Agent 3: `git-committer` — Commit ALL changes to main

**Mission**: Stage, commit, and ensure the branch is clean.

**IMPORTANT**: Wait until Agent 1 and Agent 2 have completed their work before committing.

**Tasks**:
1. Run final verification:
   ```bash
   cd server && npx tsc --noEmit  # 0 errors
   cd ../client && npx tsc --noEmit  # 0 errors
   ```
2. Check for any remaining `any` in both server and client
3. Stage all changes:
   ```bash
   git add -A
   ```
4. Review what's being committed:
   ```bash
   git diff --cached --stat
   ```
5. Do NOT commit these files (they contain secrets):
   - `.env`
   - `server/.env`
   - Any file with API keys or passwords
6. Ensure `.gitignore` excludes:
   - `cognee-repo/` (cloned external repo)
   - `scripts/_neon_*.sql` (temp migration files)
   - `.env` files
7. Create a comprehensive commit:
   ```bash
   git commit -m "feat: Phase A+B complete — 0 TS errors, typed client, Neon Cloud migration

   - Resolved all 119 TypeScript errors across server
   - Eliminated 560+ any types from server code
   - Eliminated 453 any types from client code
   - Created missing service modules (bank-reconciliation, BAS, bills, etc.)
   - Migrated 53 tables + 6520 transactions to Neon Cloud
   - Configured Docker Compose with Neon environment variables
   - Verified full Docker stack: postgres, cognee, redis, server, client
   "
   ```
8. Verify clean state:
   ```bash
   git status  # should show clean working tree (except ignored files)
   ```

**HARD DELIVERABLE**: `git status` shows clean working tree. `git log -1 --oneline` shows the commit.

---

## EXECUTION ORDER
1. Agent 1 (`any-killer-client`) starts immediately — biggest workload
2. Agent 2 (`docker-verifier`) starts immediately in parallel — independent work
3. Agent 3 (`git-committer`) waits for both to finish, then commits everything

## SUCCESS CRITERIA
- [ ] `npx tsc --noEmit` passes in BOTH server/ AND client/ (0 errors each)
- [ ] `grep -rn ': any' server/src/ client/src/ --include='*.ts' --include='*.tsx' | grep -v eslint-disable | wc -l` = 0
- [ ] `docker compose ps` shows 5 services healthy
- [ ] Neon Cloud `SELECT count(*) FROM transactions` returns 6520
- [ ] `git status` shows clean working tree
- [ ] All files committed to the branch
