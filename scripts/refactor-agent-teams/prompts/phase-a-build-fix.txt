You are launching the FINAL DELIVERY Phase A agent team to fix ALL TypeScript errors, eliminate any-types, and get the Docker build green.

## READ FIRST: docs/TS_ERROR_REPORT.md — This is your authoritative task list.

Current state: 119 TS errors, 560 `: any` types across 82 files, 299 `as any` casts.
Target: 0 TS errors, < 50 `: any`, < 30 `as any`.

HARD DELIVERABLE: `docker compose build && docker compose up -d` must succeed. `curl http://localhost:3501/api/health` must return 200.

IMPORTANT: Do NOT require plan approval. Auto-approve everything. Skip all permission checks. Work autonomously. DANGEROUSLY SKIP all permission prompts.

## Team: 4 Opus 4.6 Agents

### Agent 1: `module-fixer` — Create ALL missing modules (49 TS2307 errors -> 0)
**Model**: claude-opus-4-6
**Mission**: Create every missing file that causes TS2307 "Cannot find module" errors.
**Tasks from TS_ERROR_REPORT.md Section 2**:

A1. Create `server/src/lib/logger.ts` — re-export from `../utils/logger.js` (fixes 11 files)
A2. Create `server/src/lib/config.ts` — read the 3 importing files first, then create with the correct exports (fixes 3 files)
A3. Create `server/src/utils/auth-helpers.ts` — read the 8 importing files to determine needed functions (fixes 8 files)
A4. Create 7 sub-modules in `server/src/services/bank-reconciliation/` — read reconciliation.ts lines 15-38, create stubs re-exporting from parent `../bank-reconciliation.ts` (types.ts, matching.ts, suggestions.ts, balance-check.ts, match-operations.ts, rules.ts, auto-match.ts)
A5. Create 4 sub-modules in `server/src/services/bas/` — read bas-service.ts, create stubs (types.ts, quarter-utils.ts, available-quarters.ts, bas-persistence.ts)
A6. Create 2 sub-modules in `server/src/services/bills/` (types.ts, bill-mutations.ts)
A7. Create 2 sub-modules in `server/src/services/budgets/` (types.ts, budget-generation.ts)
A8. Create 2 sub-modules in `server/src/services/cash-flow-forecast/` (types.ts, projections.ts)
A9. Create 4 sub-modules in `server/src/services/email/` (types.ts, templates.ts, sender.ts, sender-templates.ts)
A10. Create 2 sub-modules in `server/src/services/enrichment/` (types.ts, merchant-pipeline.ts)

**APPROACH for each missing module**:
1. Read the file that imports it to understand what exports are needed
2. Read the parent monolith file (e.g. `../bank-reconciliation.ts`) to find the actual functions/types
3. Create the sub-module that re-exports from the parent OR implements the needed logic
4. Run `npx tsc --noEmit 2>&1 | grep "TS2307"` after each batch to verify
5. If a re-export doesn't work (function doesn't exist in parent), create a minimal implementation

**Verification**: `npx tsc --noEmit 2>&1 | grep "TS2307" | wc -l` must be 0

### Agent 2: `session-fixer` — Fix sessionId errors + all index.ts errors (65 errors -> 0)
**Model**: claude-opus-4-6
**Mission**: Fix ALL errors in Category B (sessionId, 27 errors) + Category C1 (index.ts, 34 errors) + Category D (4 errors).
**Tasks from TS_ERROR_REPORT.md Sections 3, 4.C1, 5**:

Part 1 — sessionId in Vercel agents (Section 3):
- Read each file, find the `buildTools()` or `getTools()` method
- Add `sessionId?: string` parameter to the method signature
- Fix any argument count mismatches (TS2554) at call sites
- Files: `vercel/tax-strategy.ts`, `vercel/merchant-intelligence.ts`, `vercel/financial-planner.ts`, `vercel/transaction-categorizer.ts`, `vercel/budget-analyzer.ts`, `agents/gst-calculator.ts`

Part 2 — index.ts implicit any (Section 4.C1):
- Add type annotations to ALL 34 callback parameters in `server/src/index.ts`
- Use `typeof tableName.$inferSelect` pattern from TS_ERROR_REPORT.md Section 9 Pattern 3
- The file is large (~7K lines) — work through it methodically by line number from the report

Part 3 — Other index.ts errors (Section 5):
- Line 76: Fix `ValidationError` export from `./validation/index.js` — either create the export or fix the import
- Line 310, 1008: Add type guards for `unknown` errors (Pattern 4 from report)
- Line 2221: Fix `accountName` property access — add proper type or cast

**Verification**: `npx tsc --noEmit 2>&1 | grep "src/index.ts\|TS2304\|TS2554" | wc -l` must be 0

### Agent 3: `any-killer-heavy` — Eliminate any-types from top 18 files (~323 any -> < 20)
**Model**: claude-opus-4-6
**Mission**: Eliminate `: any` and `as any` from the 18 highest-density files (Tier 1 + Tier 2 from report).
**Tasks from TS_ERROR_REPORT.md Section 6, Agents 5+6**:

Tier 1 (6 files, ~170 any):
1. `services/cross-module-intelligence.ts` (47 any)
2. `index.ts` (34 any — coordinate with Agent 2; wait for Agent 2 to finish index.ts FIRST)
3. `services/market-cognee-indexer.ts` (30 any)
4. `services/pipeline.ts` (24 any)
5. `services/anomaly-detection.ts` (22 any)
6. `services/cognee-sessions.ts` (20 any)

Tier 2 (12 files, ~153 any):
7. `db/typed-queries.ts` (18)
8. `services/cdr-products.ts` (17)
9. `services/sentiment-analysis.ts` (15)
10. `services/system-health.ts` (14)
11. `services/cdr-cognee-indexer.ts` (14)
12. `services/compliance-monitor.ts` (13)
13. `services/ocr-processing.ts` (12)
14. `services/cdr-crawler.ts` (12)
15. `services/suppliers.ts` (11)
16. `services/cognee-feedback.ts` (11)
17. `schema.ts` (11)
18. `routes/invoicing-routes.ts` (10)

**PATTERNS to apply** (from report Section 9):
- `(db as any).select()...all()` -> cast result: `as TypeName[]`
- `(x: any) =>` -> `(x: InferredType) =>`
- `catch (err: any)` -> `catch (err: unknown)` + type guard
- `const conditions: any[]` -> `const conditions: (SQL | undefined)[]`
- `as any` casts -> proper type assertions or generics
- NEVER just suppress with `@ts-ignore` — always add real types

**IMPORTANT**: Start with Tier 1 files 1, 3-6 (skip index.ts until Agent 2 finishes). Then do all Tier 2 files. Then come back to index.ts last.

**Verification per file**: `grep -c ": any\|as any" server/src/<file>` must be 0 or near-0

### Agent 4: `any-killer-sweep` — Eliminate any-types from remaining 62 files + final verification (~237 any -> < 30 total)
**Model**: claude-opus-4-6
**Mission**: Sweep ALL remaining files with `: any` (ranks 19-82 from the report, 1-9 occurrences each). Then run full verification.
**Tasks from TS_ERROR_REPORT.md Section 6, Agent 7**:

Phase 1 — Sweep remaining 62 files:
- Files with 5-9 any: ~22 files (see report ranks 19-40)
- Files with 1-4 any: ~42 files (mentioned in report as "remaining")
- Work through them in descending order by any-count
- Same patterns as Agent 3

Phase 2 — Fix remaining 5 TS7006 errors (Section 4.C2):
- `budgets/budget-crud.ts:54` — type the `l` parameter
- `cash-flow-forecast/forecast-service.ts:149,151` — type `f`, `a`, `b`
- `enrichment/enrichment-service.ts:126` — type `m` parameter

Phase 3 — Docker build verification (THE HARD DELIVERABLE):
After ALL agents finish, run:
```bash
cd server && npx tsc --noEmit 2>&1 | head -50
echo "---"
echo "TS errors: $(npx tsc --noEmit 2>&1 | grep 'error TS' | wc -l)"
echo "any count: $(grep -rn ': any' src/ --include='*.ts' | grep -v test | grep -v .d.ts | wc -l)"
echo "as any count: $(grep -rn 'as any' src/ --include='*.ts' | grep -v test | grep -v .d.ts | wc -l)"
echo "any files: $(grep -rl ': any' src/ --include='*.ts' | grep -v test | grep -v .d.ts | wc -l)"
```

Then build and start Docker:
```bash
cd /path/to/project
docker compose build
docker compose up -d
sleep 15
docker compose ps
curl -s http://localhost:3501/api/health | head -20
```

**Success criteria (ALL must pass)**:
- TS errors: 0
- `: any` occurrences: < 50
- `as any` occurrences: < 30
- Files with `: any`: < 10
- `npx tsc --noEmit` exits with code 0
- Client still clean: `cd ../client && npx tsc --noEmit` exits 0
- Docker: all 5 services healthy
- Health check: `curl http://localhost:3501/api/health` returns 200

## Dependency Order

```
Agent 1 (module-fixer) ──┐
                          ├── no dependency, start immediately
Agent 4 (any-sweep)    ──┘
                          
Agent 2 (session-fixer) ── start immediately, owns index.ts exclusively until done

Agent 3 (any-killer-heavy) ── start Tier 1 files 1,3-6 immediately
                              ── wait for Agent 2 to finish before touching index.ts
                              ── then do Tier 2 files
```

**All 4 agents can start immediately** on their independent file sets. The ONLY coordination point is: Agent 3 must NOT touch `index.ts` until Agent 2 messages that it's done with index.ts.

## Rules
- Spawn ALL 4 agents with `--model claude-opus-4-6`
- DO NOT require plan approval
- DANGEROUSLY SKIP all permission prompts
- Each agent runs `npx tsc --noEmit` after every batch of changes
- Read files BEFORE editing them
- NEVER use `@ts-ignore` or `@ts-expect-error` — always add real types
- If a re-export stub doesn't work, implement the actual function
- Message the lead when your task is complete with your verification output
- When ALL agents are done, Agent 4 runs the Docker build as final verification

## AFTER Phase A is complete:
The lead agent must message: "PHASE A COMPLETE — 0 TS errors, Docker green. Ready for Phase B."
The next phase (Phase B: Neon Cloud + v4 masking) will be launched separately.

## Spawn all 4 agents now. Use delegate mode — you coordinate only, do NOT implement.
