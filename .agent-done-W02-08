Agent 08 (Agent Integration) — Wave 2 Complete

## Files Modified

### 1. server/src/services/claude/base-agent.ts
- Added `import type { MutationTools }` (type-only import)
- Added `protected mutationTools?: MutationTools` optional property
- Added `setMutationTools(tools)` public setter
- Added `protected hasMutationTools()` convenience guard
- Backward-compatible: no constructor or method signature changes

### 2. server/src/services/claude/orchestrator.ts
- Added imports: `ConfirmationFlowService`, `MutationTools`
- Added `private confirmationFlow?: ConfirmationFlowService` property
- Added `initMutationFramework(db)` — called at server startup
- Added MutationTools injection in `invoke()` before `agent.invoke(input)`
- Added `getSessionMutations(sessionId)` query method
- Added `getConfirmationFlow()` accessor

### 3. server/src/services/claude/agents/transaction-categorizer.ts
- Modified `batch_categorize` tool: stores map result, proposes batch mutations
  for categorized suggestions (requiresConfirmation when confidence < 0.9)
- Modified `invoke()` override: proposes category update mutations after
  learning loop for all categorized results

### 4. server/src/services/claude/agents/gst-calculator.ts
- Modified `classify_gst_supply` tool: stores map result, proposes GST
  mutations with requiresConfirmation: true (always for financial)
- Modified `generate_bas_labels` tool: proposes BAS calculation creation
  mutation with requiresConfirmation: true

### 5. server/src/index.ts
- Added `import { orchestrator }` from claude/orchestrator
- Added `orchestrator.initMutationFramework(db)` after Wave 21 services init

## Verification
- Zero TSC errors in all 5 modified files
- 171 pre-existing errors in other modules (not from this agent's changes)
- All mutation proposals are non-blocking (try/catch with console.warn)
- GST mutations ALWAYS require confirmation (financial compliance)
- Categorization mutations auto-confirm when confidence >= 0.9
- All changes backward-compatible (optional mutationTools injection)
