Agent 4 (response-formatter-builder) completed.

Created: server/src/services/claude/response-formatter.ts

Exports:
- ChatResponse interface
- AgentDispatchResult interface
- PipelineResult interface
- ResponseFormatter class

Methods:
- formatSingle() — formats single agent result into ChatResponse
- formatPipeline() — combines multi-agent pipeline results
- formatError() — ALWAYS returns { answer: string }, NEVER { error: string }
- formatCurrency() — AUD formatting via Intl.NumberFormat (cents → dollars)
- generateFollowups() — contextual follow-ups for ALL 21 agent types + generic fallback
- extractAnswer() — cascading field extraction (summary → answer → analysis → message → generic)

Verification:
- `cd server && npx tsc --noEmit` passes clean (0 errors)
- All AgentType values from types.ts have followup mappings
- Error responses always return { answer: "..." } per API contract
