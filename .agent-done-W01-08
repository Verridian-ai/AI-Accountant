Agent 4 (response-formatter-builder) completed Task #8 — UI Chat Enhancer.

## Created Files
1. `client/src/features/chat/components/AgentResponseCard.tsx`
   - Renders agent type badge with icon + semantic color per agent
   - Displays answer text
   - Renders follow-up suggestions as clickable pill buttons
   - Maps all 21 agent types to display labels, icons, and colors

2. `client/src/features/chat/components/IntentDebugPanel.tsx`
   - Dev-only debug panel (toggled via Ctrl+Shift+D)
   - Shows intent classification, confidence bar (green/yellow/red), agent type
   - Styled with neu-inset class

3. `client/src/features/chat/components/AgentRoutingIndicator.tsx`
   - Shows agent name + spinning loader while processing
   - Human-readable agent labels for all 21 types
   - Fades in/out with opacity transition

## Modified Files
4. `client/src/api.ts`
   - Added `ChatApiResponse` interface with all enhanced fields
   - Updated `sendChatMessage()` return type from `{ answer: string }` to `ChatApiResponse`
   - Fully backward compatible (enhanced fields are optional)

5. `client/src/features/chat/components/FloatingChat.tsx`
   - Enhanced `ChatMessage` interface with type, agentType, data, intentClassification, suggestedFollowups
   - Added state: currentAgent, showDebug, lastIntent, lastAgentType
   - Uses `AgentResponseCard` for agent-typed messages, plain text for others
   - `AgentRoutingIndicator` above input during processing
   - `IntentDebugPanel` below input (Ctrl+Shift+D toggle)
   - Follow-up click triggers new chat message via `sendMessage()`
   - Ctrl+Shift+D keyboard shortcut for debug panel

6. `client/src/features/chat/components/ChatInterface.tsx`
   - Same enhancements as FloatingChat (agent response rendering, debug panel, routing indicator)
   - Follow-up click sends new message
   - Ctrl+Shift+D debug toggle

## Verification
- `cd client && npx tsc --noEmit` passes clean (0 errors)
- `cd server && npx tsc --noEmit` — only 3 pre-existing errors in compliance-monitor.ts (not my code)
- Old `{ answer }` responses render as plain text (backward compatible)
- New `{ answer, agentType, ... }` responses render with AgentResponseCard
