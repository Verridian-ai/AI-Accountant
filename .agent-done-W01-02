Agent 2 (intent-router-builder) — Task #2 COMPLETE

Created: server/src/services/claude/intent-router.ts

Exports:
- IntentClassification interface
- INTENT_CONFIDENCE_THRESHOLD constant (0.6)
- IntentRouter class

Features:
1. Two-tier classification: keyword pre-filter + Claude Haiku LLM
2. Keyword pre-filter covers: BAS/GST, payroll, depreciation, P&L/balance sheet, forecast, categorize
3. Dynamic agent discovery via orchestrator.getRegisteredAgents() with static fallback
4. buildAgentListPrompt() method — calls orchestrator when available
5. JSON response parsing with markdown fence stripping
6. Validation/normalization of all classification fields
7. Fallback to budget_analyzer with confidence 0.3 on failure
8. Conversation context support (recent transactions, account IDs, history)

Verification:
- `cd server && npx tsc --noEmit` passes clean (0 errors)
