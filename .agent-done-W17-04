W17-04: Redis Session & Cache Service — DONE

Files created:
- server/src/services/cognee-sessions.ts (CogneeSessionService class + singleton)

Files modified:
- docker-compose.yml (Redis command with AOF + maxmemory, server depends_on redis)
- server/package.json (ioredis ^5.9.2 added)

Verification:
- tsc --noEmit passes clean (no errors)
- ioredis installed with built-in TypeScript types (no @types needed)

Service features:
- Session CRUD with 30-min TTL + auto-refresh on access
- Query caching with configurable TTL + 1MB size guard
- Sliding-window rate limiting via Redis sorted sets
- Health check via Redis INFO command
- Graceful degradation: all methods return null/false/0 when Redis unavailable
- Key namespace: cognee:session:*, cognee:cache:query:*, cognee:ratelimit:*
- SCAN-based key discovery (no KEYS command = production safe)
