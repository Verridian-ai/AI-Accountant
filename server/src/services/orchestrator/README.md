# Subprocess Orchestrator (Prototype — Not Connected)

This orchestrator was designed to manage Python agent subprocesses via stdin/stdout.
It is NOT connected to any API routes — the production system uses the TypeScript
Claude agent orchestrator at `../claude/orchestrator.ts`.

This code is retained for reference but should not be used in production.
