/**
 * Sync Service — barrel re-export.
 */
export { SyncService, syncService } from './sync-service.js';
export type {
  SyncOperation,
  ApplyResult,
  SyncResult,
  ServerConflict,
  SyncLogEntry,
} from './types.js';
