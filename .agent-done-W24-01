Agent 1: PWA Schema Builder — COMPLETE
Wave 24: Mobile Responsive & PWA

## Deliverables

### 1. docker/migrations/0036_pwa_support.sql
- 3 tables: push_subscriptions, notification_preferences, offline_sync_log
- 8 indexes including partial index for pending sync items
- UNIQUE constraint on notification_preferences(user_id, tenant_id)
- UNIQUE constraint on push_subscriptions.endpoint
- FK CASCADE on all user_id and tenant_id columns

### 2. server/src/schema.ts additions
- pushSubscriptions sqliteTable with all columns
- notificationPreferences sqliteTable with all columns
- offlineSyncLog sqliteTable with all columns
- 6 type exports (Select + Insert for each)

### 3. server/src/db/postgres-schema.ts additions
- pushSubscriptions pgTable with timestamp/boolean PG types + 4 indexes
- notificationPreferences pgTable with timestamp/boolean PG types + 2 indexes
- offlineSyncLog pgTable with timestamp PG types + 4 indexes
- 6 type exports (PgSelect + NewPg for each)

## Verification
- tsc --noEmit: 0 new errors (18 pre-existing from ai/ioredis/customers/employee modules)
- All 3 tables match migration SQL structure
- FK cascades: users(id) and tenants(id) both CASCADE on DELETE

Completed: 2026-02-13
