Agent 2 (Supplier Service Builder) — COMPLETE
Timestamp: 2026-02-13

File created:
- server/src/services/suppliers.ts

SupplierService class with 7 methods:
1. listSuppliers — paginated, filterable, masked bank details
2. getSupplier — full detail with recent bills, outstanding totals, avg days to payment
3. createSupplier — UUID gen, AES-256-GCM encryption, ABN mod-89 + ABR lookup
4. updateSupplier — partial update, re-encrypt on bank change
5. archiveSupplier — soft delete with outstanding bill warning
6. searchSuppliers — autocomplete across name/contact/ABN, max 10
7. getSupplierBankDetails — internal decryption for payment processing

Key features:
- AES-256-GCM authenticated encryption (iv:authTag:encrypted hex format)
- BANK_ENCRYPTION_KEY env var (fail-fast in production)
- ABN validation: mod-89 mandatory, ABR lookup best-effort
- BSB format validation (000-000 or 6 digits)
- Lazy table loading pattern (compatible with concurrent Agent 1 schema work)
- Dual column name handling (camelCase + snake_case)

TypeScript: npx tsc --noEmit — zero errors from suppliers.ts
