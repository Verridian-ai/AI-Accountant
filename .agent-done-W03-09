Agent 9: Chat Session Integration — DONE

Files modified:
1. server/src/validation/index.ts — Added sessionId optional field to chatMessageSchema
2. server/src/services/claude/orchestrator.ts — Added imports for CogneeTools + cogneeSessionService,
   added getUserCogneeTools() and getSessionContext() methods for user-scoped operations
3. server/src/index.ts — Modified /api/chat endpoint:
   - Extracts sessionId from validated body
   - Gets user's datasetPrefix from cognee_user_accounts table
   - Gets or creates Cognee session via cogneeSessionService.getOrCreateCogneeSession()
   - Retrieves conversation history context from session
   - Passes userId to ragService.searchMulti() for user-scoped Cognee searches
   - Includes conversationHistory in combined context for AI generation
   - Records conversation turns (user + assistant) in session after response
   - Returns sessionId in response JSON for client tracking

Backward compatibility:
- Chat works without sessionId (creates new session or stateless)
- userId comes from JWT payload (existing auth flow)
- Response always includes { answer: string } (existing contract)
- sessionId in response is additive (doesn't break clients)

Verification: npx tsc --noEmit — 0 new errors in modified files (130 pre-existing from other waves)
